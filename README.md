### Basic Feed Forward Neural Network implementation using python and numpy


-> The library supports 3 activation functions
-> Specific function to define a neural network by adding n number of layers (using the in-built add_layer function)
-> It was tested on a dataset obtained from kaggle, the results showed that the model was able to bring down the loss function. But the accuracy was really low 
